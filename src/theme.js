import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      light: "skyblue",
      main: "#1760a5",
    },
    secondary: {
      main: "#15c630",
    },
    otherColor: {
      main: "#999",
    },
  },
});
